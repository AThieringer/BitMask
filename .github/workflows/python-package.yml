name: Publish Python Package to PyPI

on:
  push:
    branches:
      - master

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11"]
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name:
        run: pip install build
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev,test]
          pip install pytest

      - name: Run tests
        run: pytest

      - name: Build package
        run: build

      # - name: Publish to PyPI
      #   if: github.ref == 'refs/heads/main' && matrix.python-version == '3.11' # Only publish from one Python version
      #   run: hatch publish -u ${{ secrets.PYPI_USERNAME }} -p ${{ secrets.PYPI_TOKEN }}